# With Inheritance we don't need to create the object of all the classes.
# by just importing & creating the object of Child class, we can use the Parent class functions as well
# Const
import Inheritance3

trylogin = Inheritance3.three()
trylogin.getusername()
trylogin.getpassword()
trylogin.getsubmit()
trylogin.performlogin()
trylogin.diff(7,4,8)